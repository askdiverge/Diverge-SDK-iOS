// Optional local overrides. Copy to Secrets.xcconfig (gitignored).
// Included last so these win over Development / Production / Local.
//
// Do not put chatbot API keys here — they must not ship in the app.
// Paste a visitor JWT into the Sample's token field instead.
//
// DIVERGE_API_ENVIRONMENT = local
// DIVERGE_API_BASE_URL = http:/$()/127.0.0.1:3000
