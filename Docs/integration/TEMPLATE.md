# Integration guide — vX.Y.Z

> Copy to `Docs/integration/vX.Y.Z.md` for each iOS release that needs host-app migration steps.

## Who should read this

Host apps upgrading from `vPREV` to `vX.Y.Z` on iOS (SPM).

## Breaking changes

| Area | Before | After | Action |
|------|--------|-------|--------|
| | | | |

## Migration steps

1.
2.

## New permissions / privacy

- Info.plist keys: (none for v0.1 — see [`../privacy/`](../privacy/))
- Privacy Manifest updates:
- ATT / tracking notes:

## Verification

- [ ] Sample app builds against the new tag
- [ ] `swift test` / GitHub Actions `iOS` workflow green
- [ ] Host smoke test on iOS 18+
